$(function () {
	if ($("#portfoli-viewer").length > 0) {
		new Viewer(document.getElementById("portfoli-viewer"), {
			movable: !1,
			title: !1,
			toolbar: !1
		})
	}
}), $(function () {
	if ($("#blog-viewer").length > 0) {
		new Viewer(document.getElementById("blog-viewer"), {
			movable: !1,
			title: !1,
			toolbar: !1
		})
	}
}), $(function () {
	$(document).on("click", ".total", function () {
		$(".list_tab").addClass("act"), $(".total").addClass("act")
	}), $(document).on("click", ".total.act", function () {
		$(".list_tab").removeClass("act"), $(".total").removeClass("act")
	}), $(".list_tab").on("click", "li:not(.active)", function () {
		$(this).addClass("active").siblings().removeClass("active")
	})
}), $(function () {
	function e() {
		$(".cc-modal").fadeIn(), $("body").css("overflow", "hidden")
	}
	$(document).on("click", ".cc-container", function (e) {}), $(document).on("click", ".cc-close", function () {
		$(".cc-modal").fadeOut(), $("body").css("overflow", "unset")
	}), $(document).on("click", ".cc-modal", function (e) {}), $(document).on("click", ".cc-button", function () {
		e()
	});
	var t = document.getElementsByClassName("cc-button")[0];
	t && (t.onmousedown = function (n) {
		let o = n.clientX - t.getBoundingClientRect().left,
			c = n.clientY - t.getBoundingClientRect().top;

		function i(e, n) {
			e < window.innerWidth - 150 && e > 100 && (t.style.left = e - o + "px"), n < window.innerHeight - 40 && n > 40 && (t.style.top = n - c + "px")
		}

		function l(e) {
			(e.clientX > window.innerWidth || e.clientX < 10 || e.clientY > window.innerHeight || e.clientY < 10) && (document.removeEventListener("mousemove", l), t.onmouseup = null, document.body.style.userSelect = "unset"), i(e.clientX, e.clientY)
		}
		t.style.zIndex = 1e3, document.body.style.userSelect = "none", document.body.append(t), i(n.clientX, n.clientY), document.addEventListener("mousemove", l), t.onmouseup = function (o) {
			document.removeEventListener("mousemove", l), t.onmouseup = null, document.body.style.userSelect = "unset", n.clientY == o.clientY && n.clientX == o.clientX && e()
		}, t.ondragstart = function () {
			return !1
		}
	})
}), $(function () {
	$(".activities-nocar .activities-img").css("width", $(".owl-item.active").innerWidth())
});